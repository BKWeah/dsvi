# Environment variables for subdomain routing

# Base domain for your application
VITE_BASE_DOMAIN=yourdomain.com

# Enable/disable subdomain routing
VITE_SUBDOMAIN_ENABLED=true

# Environment (development/production)
NODE_ENV=production

# Example for development
# VITE_BASE_DOMAIN=localhost:3000
# NODE_ENV=development
